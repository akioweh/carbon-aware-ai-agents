> i almost died making this work

c++23!

dep libs:

- drogon
- boost

use clangd, clang-tidy, and clang-format.
their respect configs are tracked.

i wrote some cmake presets but idk if any works except my own (untracked) _user_ presets.

remember to generate compile_commands.json with cmake first so your tools won't spasm
